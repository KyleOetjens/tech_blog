<section class="card">
  <!-- <a href="/painting/{{id}}"> The contents of the painting card links to the painting page using the painting ID -->
  
    <!-- The card contains the painting name as the card header -->
    <header>{{comment_title}}</header>
    <!-- In the body of the card, the painting is rendered along with the artist name -->

    <div class="card-body">
      <img src="/images/{{filename}}" alt="{{description}}">
      <p>{{comment_body}}</p>
    </div>
  </a>
</section>